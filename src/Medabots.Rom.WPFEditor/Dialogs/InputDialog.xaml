<Window x:Class="Medabots.Rom.WPFEditor.Dialogs.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input"
        Width="480"
        Height="190"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#FFF7F7F7">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="PromptTextBlock"
                   FontSize="13"
                   Foreground="#FF1F1F1F"
                   TextWrapping="Wrap" />

        <TextBox x:Name="ResponseTextBox"
                 Grid.Row="1"
                 Margin="0,12,0,0"
                 MinWidth="420"
                 Padding="8,6" />

        <TextBlock x:Name="PlaceholderTextBlock"
                   Grid.Row="2"
                   Margin="0,8,0,0"
                   FontSize="11"
                   Foreground="#FF6B7280"
                   TextWrapping="Wrap" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,16,0,0">
            <Button x:Name="OkButton"
                    Width="96"
                    Margin="0,0,8,0"
                    IsDefault="True"
                    Click="OnOkClicked">OK</Button>
            <Button Width="96"
                    IsCancel="True">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>